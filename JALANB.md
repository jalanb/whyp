# Whyp

`whyp` is a `shell` tool that expands on `type` 

`whyp` is written in `bash`, with some `Python` extensions`

## Functions

`whyp` adds a number of `bash` `functions` to the user's shell
- `e`
- `w`
- `ww`
- `whyp`

These, and other implementation details are in `whyp.sh`

